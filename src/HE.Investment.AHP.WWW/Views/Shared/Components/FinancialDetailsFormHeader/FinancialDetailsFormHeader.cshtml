@model (string Title, string Caption, string ApplicationId, HE.Investment.AHP.Domain.FinancialDetails.FinancialDetailsWorkflowState CurrentPage)
@{
    var redirectParameters = new { applicationId = Model.ApplicationId, currentPage = Model.CurrentPage };
}

<gds-back-link href="@Url.Action("Back", "FinancialDetails", redirectParameters)" text="Back" class="govuk-!-margin-bottom-8"></gds-back-link>

@await Html.PartialAsync("~/Partials/Errors/_ErrorSummaryPartial.cshtml")

<vc:form-header title="@Model.Title" caption="@Model.Caption"/>
