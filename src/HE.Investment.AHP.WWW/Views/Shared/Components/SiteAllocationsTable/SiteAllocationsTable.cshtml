@using HE.Investments.Common.WWW.Components.Table

@model (HE.Investment.AHP.Contract.Site.SiteId siteId, List<TableHeaderViewModel> headers, List<TableRowViewModel> rows);

    <vc:table
        headers="@Model.headers"
        rows="@Model.rows"/>
