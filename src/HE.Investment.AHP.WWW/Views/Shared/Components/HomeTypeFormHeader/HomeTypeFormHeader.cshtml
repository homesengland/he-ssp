@model (string Title, string Caption, HE.Investment.AHP.Contract.HomeTypes.HomeTypesWorkflowState CurrentPage, bool IsReadOnly)

<vc:ahp-workflow-back-button current-page="@Model.CurrentPage"/>

@if (Model.IsReadOnly)
{
    <gds-div-inset-text>
        Your application is now locked. Make a request to your Growth Manager if you need to make any changes.
    </gds-div-inset-text>
}

@await Html.PartialAsync("~/Partials/Errors/_ErrorSummaryPartial.cshtml")

<vc:page-header title="@Model.Title" caption="@Model.Caption"/>
