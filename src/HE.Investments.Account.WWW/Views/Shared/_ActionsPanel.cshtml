@model IList<HE.Investments.Common.WWW.Models.ActionModel>
@if (Model.Any())
{
    <hr class="separator"/>
    @foreach (var action in Model)
    {
        <strong class="govuk-!-display-inline-block govuk-!-padding-top-3">
            <a href="@(action.HasAccess ? Url.Action(action.Action, action.Controller, action.Values ) : null)" class="@(action.HasAccess ? null : "link-disabled")">@action.Label</a>
        </strong>
    }
}
