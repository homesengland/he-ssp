@using HE.InvestmentLoans.Contract.Application.Enums
@model ApplicationStatus

@if (Model is ApplicationStatus.ApplicationSubmitted
                    or ApplicationStatus.ApplicationUnderReview
                    or ApplicationStatus.ReferredBackToApplicant)
{
    <gds-div-inset-text>
        If you want to update your application, select the section you want to make a change or changes in.
        You will need to save these to confirm your update.
    </gds-div-inset-text>
}
